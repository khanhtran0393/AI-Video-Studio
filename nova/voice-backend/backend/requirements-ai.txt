# Model AI thật. CẦN Python 3.11 (PyTorch chưa có bản cho 3.14).
# Lưu ý tương thích: omnivoice bắt buộc transformers>=5.3; coqui-tts 0.27.5 xoay quanh
# transformers 4.x → engine xtts tự shim (engines/tts_patch.py) để chạy được với 5.x.
torch==2.13.0
torchaudio==2.11.0
torchcodec
coqui-tts==0.27.5
transformers>=5.3.0
huggingface_hub
# OmniVoice — đa ngôn ngữ, clone giọng + thiết kế giọng theo thuộc tính (engine mặc định):
omnivoice
num2words
# VieNeu-TTS — tiếng Việt native + voice cloning (v3 Turbo 48kHz, CPU qua ONNX):
vieneu
# ASR — Apple Silicon dùng mlx-whisper (GPU Metal); nền khác dùng faster-whisper:
mlx-whisper; sys_platform == "darwin" and platform_machine == "arm64"
faster-whisper>=1.0
