# Packaged application and generated artifacts
AI Video Studio.exe
Uninstall AI Video Studio.exe
build-output/
resources/
locales/
resources/app/
resources/*.pak
resources/*.bin
resources/*.dll
resources/*.exe

# Electron runtime files unpacked beside the packaged app
*.pak
*.bin
*.dll
*.exe
LICENSE.electron.txt
LICENSES.chromium.html
icudtl.dat
vk_swiftshader_icd.json
resources/*.json

# Dependency trees and generated build state
**/node_modules/
**/dist/
**/out/
**/coverage/
smoke-results/
*.asar
*.blockmap

# Runtime data, logs and local secrets
*.log
*.tmp
*.cache
.env
.env.*
!.env.example
**/userData/
**/chrome-accounts/
**/*-accounts.json
**/*-settings.json
**/*-cookies.json
**/*-tokens.json

# OS/editor files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Temporary rebranding inspection artifacts; never commit source snapshots/reports.
rebrand-backup/
rebrand-report.txt

# Downloaded runtime binaries (yt-dlp, upscaler, inpaint, sqlite, onnx)
nova/*-bin/

# Python bytecode caches
__pycache__/
*.pyc

# Voice backend runtime dir (regenerated by start.bat from nova/voice-backend).
# Contains .venv-omni (~2GB), downloaded HF models and generated audio.
nova/voice-studio/

# Keep the Auto-Fix control plane and its documentation tracked.
!auto-fix/
!auto-fix/**

# Temporary generated data
_tmp_*
venv*/
*.wav
nova/voice-backend/data/
*.err
.tmp*
nova_scan_tmp*

# nova/chrome-extension/ — OUTPUT runtime của IPC 'flow-ext-export' (cpSync từ
# nova/flow-extension khi dev; Chrome tự sinh thêm _metadata/ khi load). KHÔNG phải
# nguồn — không track. Nguồn extension: nova/flow-extension (chính) + nova/nova-studio (biến thể riêng).
nova/chrome-extension/
artifacts/
output/
tts-demo/
tmp*
nova/tmp*
build-wt/
*.mp3
*.mp4
